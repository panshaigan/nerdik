# Copy to Caddyfile and set APP_DOMAIN / ACME_EMAIL in .env (see docs/deployment.md).
# Caddy loads environment variables from the caddy service in compose.prod.yaml.

{
	email {$ACME_EMAIL}
}

{$APP_DOMAIN} {
	encode gzip

	handle /app/* {
		reverse_proxy reverb:8080
	}

	reverse_proxy app:80
}
