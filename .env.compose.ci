# Minimal env for CI compose config validation (not used at runtime).
NERDIK_IMAGE=ghcr.io/example/nerdik:ci
GITHUB_OWNER=example
DB_PASSWORD=secret
APP_DOMAIN=localhost
STAGING_DOMAIN=staging.example.com
ACME_EMAIL=ci@example.com
